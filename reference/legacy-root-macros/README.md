# 历史 ROOT 参考宏

这个目录存放旧版 ROOT 宏，仅用于：

- 对照早期物理想法和输出形态
- 回看曾经踩过的问题
- 辅助理解当前实现为什么要保持 ROOT-free 核心

约束：

- 不参与 CMake 构建
- 不作为当前功能入口
- 不应直接复制代码到 `src/`、`include/` 或 `apps/`

如果需要新增参考材料，也应继续放在这个目录下，而不是回到仓库顶层。
